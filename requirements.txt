tensorflow>=2.15,<2.18          # TF + tf.lite (CPU par défaut; GPU si CUDA/cuDNN dispos)
numpy                           # Compat TF 2.15+ (évite NumPy 2.x)
pandas                          # DataFrames / analyses
scipy                           # Stats (zscore, etc.)
scikit-learn                    # Splits, métriques
pydot                            # Visualisation du modèle (plot_model)

matplotlib                      # Plots
seaborn                         # Stats viz
pillow                          # PIL
opencv-python                   # CLAHE / rotations (cv2)
tqdm                            # Progress bars

# Notebook tooling
jupyter                         # Jupyter + notebook
ipykernel                       # Kernel VS Code/Jupyter
ipywidgets                      # Widgets
nbformat                        # Manipulation programmatique des notebooks

# Données
kaggle                          # Téléchargement dataset UTKFace via API
